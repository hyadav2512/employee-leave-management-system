function LeaveFilters({ filters, types, onChange, onClear }) {
  return <div className="leave-filters"><label className="search-field">Search<input type="search" placeholder="Search leave requests..." value={filters.search} onChange={(event) => onChange('search', event.target.value)} /></label><label>Status<select value={filters.status} onChange={(event) => onChange('status', event.target.value)}><option value="">All statuses</option>{['Pending', 'Approved', 'Rejected', 'Cancelled'].map((status) => <option key={status}>{status}</option>)}</select></label><label>Leave type<select value={filters.leaveType} onChange={(event) => onChange('leaveType', event.target.value)}><option value="">All leave types</option>{types.map((type) => <option key={type.id} value={type.name}>{type.name}</option>)}</select></label><label>From<input type="date" value={filters.startDate} onChange={(event) => onChange('startDate', event.target.value)} /></label><label>To<input type="date" value={filters.endDate} onChange={(event) => onChange('endDate', event.target.value)} /></label><label>Sort by<select value={filters.sortBy} onChange={(event) => onChange('sortBy', event.target.value)}><option value="createdAt">Applied date</option><option value="startDate">Start date</option><option value="endDate">End date</option></select></label><button type="button" className="sort-order" onClick={() => onChange('sortOrder', filters.sortOrder === 'asc' ? 'desc' : 'asc')}>{filters.sortOrder === 'asc' ? 'Oldest first' : 'Newest first'}</button><button type="button" className="clear-filters" onClick={onClear}>Clear filters</button></div>;
}
export default LeaveFilters;
