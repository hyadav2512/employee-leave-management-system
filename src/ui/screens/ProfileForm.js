function ProfileForm({ profile, form, errors, saving, onChange, onSubmit, onImageChange, imagePreview, onRemoveImage }) {
  const readOnly = [['email', 'Email'], ['employeeId', 'Employee ID'], ['department', 'Department'], ['designation', 'Designation'], ['manager', 'Manager'], ['joiningDate', 'Joining date'], ['role', 'Role']];
  return <form className="profile-form" onSubmit={onSubmit} noValidate><div className="avatar-row"><div className="profile-avatar">{imagePreview ? <img src={imagePreview} alt="Profile preview" /> : profile.name?.slice(0, 2).toUpperCase()}</div><div><label className="upload-button" htmlFor="profileImage">Change photo<input id="profileImage" type="file" accept="image/jpeg,image/png,image/webp" onChange={onImageChange} /></label>{imagePreview && <button className="text-button" type="button" onClick={onRemoveImage}>Remove</button>}<p className="field-hint">JPEG, PNG or WEBP, max 1.5 MB</p></div></div><div className="profile-fields"><label>Full name<input name="name" value={form.name} onChange={onChange} aria-invalid={Boolean(errors.name)} />{errors.name && <span className="profile-field-error">{errors.name}</span>}</label><label>Phone number<input name="phone" inputMode="numeric" value={form.phone} onChange={onChange} aria-invalid={Boolean(errors.phone)} />{errors.phone && <span className="profile-field-error">{errors.phone}</span>}</label>{readOnly.map(([name, label]) => <label key={name}>{label}<input value={form[name] || ''} readOnly className="read-only" /></label>)}</div><div className="profile-actions"><button className="apply-primary" type="submit" disabled={saving}>{saving ? 'Saving...' : 'Save changes'}</button></div></form>;
}
export default ProfileForm;
